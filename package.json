{
  "name": "una-home-fullstack",
  "version": "1.0.0",
  "description": "UNA Home & Furniture Cloudflare Worker + D1 website",
  "type": "module",
  "main": "worker/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
