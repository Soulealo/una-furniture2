# Cloudflare D1 is configured in wrangler.toml.
# For production, create a real database with:
# npx wrangler d1 create unafurniture
# Then replace database_id in wrangler.toml with the generated ID.
